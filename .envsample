# Azure Config
##################################################
AZURE_LOCATION=''
AZURE_APPENV=''
AZURE_SUBSCRIPTIONID=''
# Specify a username unique to you (i.e., not admin, root, etc.)
AZURE_RESOURCE_USERNAME=''
AZURE_RESOURCE_EMAIL=''
AZURE_RESOURCE_PASSWORD=''

# Current User Config
##################################################
# Can get from here: http://ifconfig.me/ip
PUBLIC_IP_ADDRESS=''

# Website Config
##################################################
ROOT_DOMAIN=''
# Use `base64 -w 0 FILE_NAME` (or `base64 -i FILE_NAME` on macOS) command to generate
CERTIFICATE_BASE64_STRING=''
CERTIFICATE_PASSWORD=''

# Conditional Deployments (for debugging)
##################################################
UPLOAD_CERTIFICATE='true'
DEPLOY_MANAGEMENT='true'
DEPLOY_SECURITY='true'
DEPLOY_NETWORKING='true'
DEPLOY_DATA='true'
DEPLOY_COMPUTE='true'
